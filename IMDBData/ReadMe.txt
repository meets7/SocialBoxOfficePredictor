In terminal change to the directory where your jar file is loaded.
Run the jar file using the below command. Please note that the file accept one directory path as argument. The path should hold the Text file “MovieList.txt” containing all the movie list you need data for.
After the program successfully runs it will generate a csv file in the same path.

cd /Users/AshishMAC/Desktop
java -jar ParseJSON.jar "/Users/AshishMAC/Desktop"